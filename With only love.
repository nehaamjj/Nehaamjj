<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Happy Birthday ❤️ — Your Neha</title>
  <style>
    :root{
      --bg1:#fff6f9;
      --bg2:#fff0f6;
      --accent1:#ff6fa8;
      --accent2:#ffc3dd;
      --text:#3b3b3b;
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      min-height:100vh;
      display:flex;
      align-items:center;
      justify-content:center;
      font-family: "Poppins", "Inter", system-ui, -apple-system, Arial;
      background: linear-gradient(180deg,var(--bg1),var(--bg2));
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
      overflow:hidden;
    }
    .card{
      width:92%;
      max-width:720px;
      background:#fff;
      border-radius:20px;
      box-shadow:0 18px 40px rgba(15,23,42,0.12);
      padding:34px;
      position:relative;
      text-align:center;
      overflow:visible;
    }
    h1{
      margin:6px 0 10px;
      color:var(--accent1);
      font-size:28px;
      letter-spacing:0.2px;
    }
    .subtitle{color:var(--text);margin:0 0 18px;font-size:14px}
    #message{display:none;text-align:left;color:var(--text);white-space:pre-wrap;font-size:15px;line-height:1.6;margin-top:18px;padding:6px 4px}
    .btn{
      display:inline-block;
      margin-top:8px;
      background:linear-gradient(90deg,var(--accent1),var(--accent2));
      color:white;padding:12px 22px;border-radius:12px;border:0;font-weight:700;font-size:15px;cursor:pointer;
      box-shadow:0 8px 22px rgba(255,111,166,0.22);
    }
    .btn:active{transform:scale(.99)}
    .glow{
      pointer-events:none;
      position:absolute;
      inset:0;
      overflow:hidden;
      z-index:0;
    }
    .heart{
      position:absolute;
      width:36px;height:36px;opacity:0.0;
      transform:translateY(10px) scale(.6);
      filter:drop-shadow(0 6px 18px rgba(255,105,161,0.18));
      animation: floatUp 6s linear infinite;
    }
    .heart svg{width:100%;height:100%}
    @keyframes floatUp{
      0%{opacity:0; transform:translateY(10px) scale(.6)}
      10%{opacity:1}
      50%{opacity:0.9; transform:translateY(-80px) scale(1)}
      100%{opacity:0; transform:translateY(-220px) scale(1.1)}
    }
    .grad {
      background: linear-gradient(90deg,#ff6fa8 0%, #ffb4d3 50%, #ffd8e9 100%);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
      font-weight:600;
    }
    #confetti {
      position:fixed; left:0; top:0; width:100%; height:100%; pointer-events:none; z-index:5;
    }
    @media (max-width:520px){
      .card{padding:22px}
      h1{font-size:22px}
    }
  </style>
</head>
<body>
  <audio id="bgm" loop preload="auto">
    <source src="https://cdn.pixabay.com/download/audio/2021/08/04/audio_1bfa1a7b9f.mp3?filename=soft-piano-ambient-6377.mp3" type="audio/mpeg">
  </audio>
  <audio id="typeSound" preload="auto">
    <source src="https://cdn.jsdelivr.net/gh/anars/blank-audio/1-second-of-silence.mp3" type="audio/mpeg">
  </audio>

  <canvas id="confetti"></canvas>

  <div class="card" role="main">
    <div class="glow" aria-hidden="true" id="glowLayer"></div>

    <h1 class="grad">Happy Birthday my ❤️</h1>
    <p class="subtitle">Open your surprise — a message just for you ✨</p>

    <button class="btn" id="openBtn">Open your message 💌</button>

    <div id="message" aria-live="polite"></div>
  </div>

  <script>
    const MESSAGE = `Happy Birthday my ❤️

You know what, you inspire me in ways you don’t even realize…
Always a fan of your logic and thinking (but not unless and until you start being practical in sensitive things 😅😂😝)
Anywaysss,
I just loveeeee youuuu yaaaaaaaar 💃🤪
I’m always so proud of you! The way you are dedicated towards your work, career etc truly inspires me and honestly, it really really does…

I know we have argued and sometimes, I talked in the most harshest tone 🙃
But yaar, I’m really sorry… when I get super duper emotional, I don’t even realize what I’m saying 👉👈😅🙃
So sorry for all those things.
I know I need to work on that and I will, that’s for sureee 💗

May God always bless you with everything you truly deserve — success, achievements, good health, happiness, and always keep you safe 🧿

Ummaaaaaaaaaaah 💋.

Yours Neha 💃💅`;

    const openBtn = document.getElementById('openBtn');
    const msgEl = document.getElementById('message');
    const bgm = document.getElementById('bgm');
    const typeSound = document.getElementById('typeSound');
    const confettiCanvas = document.getElementById('confetti');
    const glowLayer = document.getElementById('glowLayer');

    const heartsCount = 7;
    for(let i=0;i<heartsCount;i++){
      const wrap = document.createElement('div');
      wrap.className = 'heart';
      wrap.style.left = (5 + Math.random()*90) + '%';
      wrap.style.bottom = (-10 - Math.random()*40) + 'px';
      wrap.style.animationDelay = (Math.random()*4) + 's';
      wrap.style.width = (18 + Math.random()*30) + 'px';
      wrap.style.height = wrap.style.width;
      wrap.innerHTML = `<svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M12 21s-7.5-4.35-9.2-6.1C-0.4 11.9 3 7.9 6 6.2 8.2 5 10.3 5.4 12 7.1c1.7-1.7 3.8-2.1 6-1 3 1.7 6.4 5.7 3.2 8.8C19.5 16.65 12 21 12 21z" fill="#ff8cb0" /></svg>`;
      glowLayer.appendChild(wrap);
    }

    const c = confettiCanvas;
    const ctx = c.getContext('2d');
    function resizeCanvas(){ c.width = innerWidth; c.height = innerHeight; }
    resizeCanvas(); window.addEventListener('resize', resizeCanvas);
    const confettiPieces = [];
    function createConfetti(){
      for(let i=0;i<120;i++){
        confettiPieces.push({
          x: Math.random()*c.width,
          y: -Math.random()*c.height,
          r: 6 + Math.random()*10,
          vx: (Math.random()-0.5)*1.2,
          vy: 2 + Math.random()*4,
          color: ['#ffb6d9','#ffd7eb','#ffcbe6','#ff9cc4'][Math.floor(Math.random()*4)],
          rot: Math.random()*360,
          vr: (Math.random()-0.5)*6
        });
      }
    }
    function drawConfetti(){
      ctx.clearRect(0,0,c.width,c.height);
      confettiPieces.forEach((p)=>{
        p.x += p.vx; p.y += p.vy; p.rot += p.vr;
        ctx.save();
        ctx.translate(p.x,p.y);
        ctx.rotate(p.rot*Math.PI/180);
        ctx.fillStyle = p.color;
        ctx.fillRect(-p.r/2,-p.r/2,p.r,p.r*1.6);
        ctx.restore();
      });
    }
    let confettiRunning = false;
    function runConfetti(){
      if(confettiRunning) return;
      confettiRunning = true;
      createConfetti();
      let frames = 0;
      const id = setInterval(()=>{
        drawConfetti();
        frames++;
        if(frames>200){
          clearInterval(id);
          confettiPieces.length = 0;
          ctx.clearRect(0,0,c.width,c.height);
          confettiRunning = false;
        }
      }, 16);
    }

    function typeText(text, element){
      element.style.display = 'block';
      element.textContent = '';
      const chars = text.split('');
      let i = 0;
      const speed = 26;
      const soundInterval = 3;
      const typer = setInterval(()=>{
        element.textContent += chars[i];
        if(i % soundInterval === 0){
          try{ typeSound.currentTime = 0; typeSound.play(); }catch(e){}
        }
        i++;
        if(i >= chars.length) clearInterval(typer);
      }, speed);
    }

    function applyGradientDuringTyping(el){
      el.classList.add('grad');
      setTimeout(()=> el.classList.remove('grad'), 6000);
    }

    openBtn.addEventListener('click', async ()=>{
      try{ bgm.currentTime = 0; await bgm.play(); }catch(e){}
      openBtn.style.transform = 'scale(0.96)';
      setTimeout(()=> openBtn.style.display='none',220);
      runConfetti();
      applyGradientDuringTyping(document.querySelector('h1'));
      typeText(MESSAGE, msgEl);
    });

    openBtn.addEventListener('keydown', (e)=>{ if(e.key === 'Enter' || e.key === ' ') openBtn.click(); });
    confettiCanvas.style.zIndex = 7;
    document.querySelector('.card').style.zIndex = 10;
  </script>
</body>
</html>
