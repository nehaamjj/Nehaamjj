<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Happy Birthday 🎉</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: 'Comic Sans MS', cursive, sans-serif;
      background: radial-gradient(circle at top left, #ffe6f0, #ffcccc, #fff0f5);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      overflow: hidden;
      text-align: center;
      position: relative;
    }

    .card {
      background: #fff8ff;
      border: 3px dashed #ff69b4;
      border-radius: 25px;
      padding: 40px 30px;
      max-width: 600px;
      box-shadow: 0 8px 25px rgba(255,105,180,0.3);
      animation: popIn 1s ease-out;
      position: relative;
      z-index: 2;
    }

    h1 {
      color: #ff1493;
      font-size: 3rem;
      margin-bottom: 20px;
      text-shadow: 1px 1px #ffb6c1;
    }

    p {
      font-size: 1.2rem;
      line-height: 1.7;
      color: #333;
      margin: 12px 0;
    }

    .emoji {
      font-size: 2rem;
    }

    @keyframes popIn {
      0% { transform: scale(0.5); opacity: 0; }
      100% { transform: scale(1); opacity: 1; }
    }

    /* Confetti styles */
    .confetti {
      position: absolute;
      width: 10px;
      height: 10px;
      background-color: red;
      opacity: 0.7;
      z-index: 1;
      animation: fall linear infinite;
    }

    @keyframes fall {
      0% { transform: translateY(-50px) rotate(0deg); }
      100% { transform: translateY(110vh) rotate(360deg); }
    }
  </style>
</head>
<body>

  <!-- Confetti elements -->
  <script>
    const colors = ["#ff4b5c","#ff1493","#ff69b4","#ffb6c1","#ffc0cb"];
    for(let i=0;i<100;i++){
      const confetti = document.createElement('div');
      confetti.classList.add('confetti');
      confetti.style.left = Math.random()*100 + 'vw';
      confetti.style.backgroundColor = colors[Math.floor(Math.random()*colors.length)];
      confetti.style.animationDuration = (Math.random()*3 + 2) + 's';
      confetti.style.width = confetti.style.height = (Math.random()*8 + 5) + 'px';
      confetti.style.opacity = Math.random();
      document.body.appendChild(confetti);
    }
  </script>

  <div class="card">
    <h1>🎉 Happy Birthday my ❤️ 🎉</h1>
    <p>You know what, you inspire me in ways you don’t even realize…</p>
    <p>Always a fan of your logic and thinking (but not unless and until you start being practical in sensitive things 😅😂😝)</p>
    <p>Anywaysss, I just loveeeee youuuu yaaaaaaaar 💃🤪</p>
    <p>I’m always so proud of you! The way you are dedicated towards your work, career etc truly inspires me and honestly, it really really does…</p>
    <p>I know we have argued and sometimes, I talked in the harshest tone 🙃<br>
       But yaar, I’m really sorry… when I get super duper emotional, I don’t even realize what I’m saying 👉👈😅🙃<br>
       So sorry for all those things. I know I need to work on that and I will, that’s for sureee 💗</p>
    <p>May God always bless you with everything you truly deserve — success, achievements, good health, happiness, and always keep you safe 🧿</p>
    <p>Ummaaaaaaaaaaah 💋</p>
    <p>Yours, <strong>Neha 💃💅</strong></p>
  </div>

  <!-- Birthday music -->
  <audio autoplay>
    <source src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3" type="audio/mpeg">
  </audio>

</body>
      </html>
